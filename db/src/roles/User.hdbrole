{
  "role": {
    "name": "User",
    "schema_privileges": [
      {
        "privileges": [
          "CREATE TEMPORARY TABLE",
          "DELETE",
          "INSERT",
          "SELECT",
          "UPDATE",
          "SELECT CDS METADATA",
          "SELECT METADATA"
        ]
      }
    ]
  }
}